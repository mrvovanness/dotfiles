if
  (is (application_name) "Thunderbird")
  (begin
    (set_workspace 2)
    (undecorate)
    (geometry "960x1080+0+0")
    (focus))
if (matches (application_name) "^Skype")
  (begin
    (focus)
    (set_workspace 2))
