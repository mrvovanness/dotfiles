if
  (and
    (is (window_class) "Gnome-terminal")
    (is (window_name) "shell"))
  (begin
    (maximize_vertically)
    (geometry "960x1080+0+0")
    (undecorate)
    (set_workspace 1)
    (focus))
